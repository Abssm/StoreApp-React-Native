# StoreApp-React-Native
StoreApp is a React Native (CLI) mobile app starter for store-style apps. It includes Welcome/Login and Sign Up screens, reusable UI components, light/dark theming, and a demo auth flow that checks a public users API. Fake logins show an alert and redirect to Sign Up, then return to Welcome for final login.
